import{useCallback,useEffect,useState}from'react';import{api}from'./client';import type{MarketDecision}from'./contracts';export function useIntelligence(symbol='BTCUSDT',pollMs=15000){const[data,setData]=useState<MarketDecision|null>(null);const[loading,setLoading]=useState(true);const[error,setError]=useState<string|null>(null);const[updatedAt,setUpdatedAt]=useState<number|null>(null);const load=useCallback(async()=>{try{const next=await api.decision(symbol);setData(next);setError(null);setUpdatedAt(Date.now())}catch(e){setError(e instanceof Error?e.message:'Intelligence unavailable')}finally{setLoading(false)}},[symbol]);useEffect(()=>{load();const id=window.setInterval(load,pollMs);return()=>window.clearInterval(id)},[load,pollMs]);return{data,loading,error,updatedAt,refresh:load}}
