import{useApi}from'../api/useApi';import{api}from'../api/client';import{useI18n}from'../i18n';
export function PerformanceCenter(){
  const{t}=useI18n();
  const{data,loading,error}=useApi(()=>api.performance('BTCUSDT'),[],30000);
  return <div className="page"><div className="page-head"><div><span className="eyebrow">{t.pcEyebrow}</span><h1>{t.pcTitle}</h1><p>{t.pcSub}</p></div><div className="system-pill"><i/>{t.pcOutcomesCurrent}</div></div>{loading&&!data&&<section className="panel empty-state"><h2>{t.pcLoading}</h2></section>}{!loading&&(!data||data.length===0)&&<section className="panel empty-state"><h2>{t.pcNoSamples}</h2><p>{t.pcNoSamplesDetail}</p></section>}{data&&data.length>0&&<><section className="performance-hero-grid">{data.map(h=><article className="performance-horizon-card" key={h.horizon}><span>{h.horizon.toUpperCase()} · {t.pcWinRate}</span><strong>{h.winRate.toFixed(1)}%</strong><small>{h.samples} {t.pcRealizedSamples}</small><div className="performance-return"><span>{t.pcAvgReturn}</span><b>{h.avgDirectionalReturnPct>=0?'+':''}{h.avgDirectionalReturnPct.toFixed(2)}%</b></div></article>)}</section><div className="dashboard-split"><section className="panel"><span className="eyebrow">{t.pcModelPerformance}</span><h2>{t.pcByHorizon}</h2><div className="stat-list">{data.map(h=><div key={h.horizon}><span>{h.horizon.toUpperCase()}</span><b>{h.winRate.toFixed(1)}% · {h.samples} {t.pcSamples}</b></div>)}</div></section><section className="panel"><span className="eyebrow">{t.pcDirectionQuality}</span><h2>{t.pcReturnBreakdown}</h2><div className="stat-list">{data.map(h=><div key={h.horizon}><span>{h.horizon.toUpperCase()} {t.pcAvgReturn}</span><b>{h.avgDirectionalReturnPct>=0?'+':''}{h.avgDirectionalReturnPct.toFixed(2)}%</b></div>)}<div><span>{t.pcReliability}</span><b>{data[0]?.reliability}</b></div></div></section></div></>}{error&&<p className="auth-error">{error}</p>}</div>
}
