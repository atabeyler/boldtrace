import {useState,type ReactNode} from 'react';import {Brand} from './Brand';import {LanguageSelector} from './LanguageSelector';import {Footer} from './Footer';import {navigation} from '../navigation';
export function AppShell({children}:{children:ReactNode}){const[collapsed,setCollapsed]=useState(false);const[active,setActive]=useState('command');const[notifications,setNotifications]=useState(false);const[profile,setProfile]=useState(false);return <div className={`app-shell ${collapsed?'is-collapsed':''}`}><aside className="sidebar"><div className="sidebar-brand"><Brand compact={collapsed}/><button className="icon-button" onClick={()=>setCollapsed(v=>!v)} aria-label="Toggle menu">{collapsed?'›':'‹'}</button></div><nav>{navigation.map(item=><button key={item.id} className={active===item.id?'nav-item active':'nav-item'} onClick={()=>setActive(item.id)} title={item.label}><span className="nav-icon">{item.icon}</span>{!collapsed&&<span>{item.label}</span>}</button>)}</nav><div className="sidebar-status"><i/><>{!collapsed&&<span>Intelligence Online</span>}</></div></aside><div className="app-column"><header className="topbar"><div className="market-state"><span className="pulse"/>LIVE MARKET INTELLIGENCE</div><div className="top-actions"><LanguageSelector/><button className="icon-button notification-button" onClick={()=>setNotifications(v=>!v)} aria-label="Notifications">◉<b>3</b></button><button className="profile-button" onClick={()=>setProfile(v=>!v)}><span>BT</span><div><strong>Operator</strong><small>Secure session</small></div></button></div>{notifications&&<div className="popover notifications"><h3>Intelligence Alerts</h3><p><b>BTCUSDT</b> confidence crossed 80%</p><p><b>ETHUSDT</b> regime transition detected</p><p><b>SYSTEM</b> all market feeds healthy</p><button>Open Alert Center</button></div>}{profile&&<div className="popover profile-menu"><button>Profile & Account</button><button>Security & Sessions</button><button>Settings</button><hr/><button>Sign out</button></div>}</header><main className="content">{children}</main><Footer/></div></div>}
